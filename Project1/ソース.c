#include<stdio.h>
//プロトタイプ宣言

void main(void) {
	int x;
	//xは整数型（int）。
	//signed_intの範囲は2Byte-32768～32767、4Byteは-2147483648～2147483647
	//unsignedは即ち符号なし。＋の数字のみ使える。
	double y, z;
	//y,zは倍精度実数型(double)使用幅は8Byte
	//範囲は1.7E-308~1.7E+308
	float f;
	//fは単精度実数型(float)。使用幅は4Byte
	//範囲は3.4E-38~3.4E+38
	short s;
	//sは短整数型（short）。使用幅は2Byte。
	//unsignだと65535まで使用可能。
	long l;
	//倍長整数型（long）。使用幅は4~8Byte。
	//unsignedだと4Byteで4294967295、8Byteで18446744073709551615まで
	char:'a'
	//charは文字型、文字列の場合は最後にnull文字が入っているのでx+1として換算
	//文字型データと文字列は違う。
　　//使用するバイト幅は１Byte。表現範囲は-128～127となる。
	

}
//１Byte8ビットである。8ビット＝２５６忘れずに。